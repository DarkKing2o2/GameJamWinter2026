extends Node
## Global Enum script
## Use for accessing Enums used in multiple scrips and keeping them consistent

## Enum that contains the possible stats a player has [br]
## Each stat has an integer value to use with find_key for randomization
enum Stats {
	FIRE_RATE = 0,
	SPEED = 1,
	DETECTION_RANGE = 2,
	VITALITY = 3,
	}
